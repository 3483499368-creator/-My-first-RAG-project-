# AI 求职智能 Agent 平台 · 面试面经模板

> 本模板基于项目复盘文档整理，按"必答模块 → 深化追问 → 避坑清单"组织。
> 标注 `[需核对]` 的字段表示必须从源码 / 配置中确认真实值后方可作答，不可编造。

---

## 〇 一句话项目定位（电梯陈述模板）

> "我做的项目是一个 **AI 求职智能 Agent 平台**，核心能力是基于用户画像对招聘网站岗位做**规则初筛 + 大模型深度核验**的匹配度分析。
> 架构上是 **Userscript 采集 + React 后台 + Spring Boot 后端 + MySQL/Redis 存储** 的混合方案，
> 关键设计点是用 **profileVersion 机制** 解决画像更新时的缓存失效问题，用 **画像分块（profile_chunk）** 替代整份简历存储以支持增量更新与按类型检索。"

---

## 一、系统架构与数据流（高频必答）

### 1.1 整体架构（四层）

| 层级 | 技术栈 | 职责 |
| :--- | :--- | :--- |
| 采集前端 | Userscript | 注入招聘网站，劫持 DOM 提取岗位信息 |
| 管理后台 | React + TypeScript + Vite + AntDesign | 用户画像维护、历史记录管理 |
| 后端服务 | Spring Boot | 缓存查询、画像召回、Prompt 组装、模型调用、结果持久化 |
| 存储层 | MySQL（持久化） + Redis（缓存） | 结构化数据持久化 + 分析结果缓存 |

### 1.2 七步闭环数据流（必须能口述完整链路）

```
[采集] Userscript 提取岗位字段
   ↓
[初筛] 前端规则评分 + 风险标记
   ↓
[请求] POST /api/job/analyze  (JSON: 岗位信息 + 前端评分明细)
   ↓
[缓存] Redis Key = job:analysis:{profileVersion}:{jobHash}  → 命中则直接返回
   ↓
[召回] 缓存未命中 → 按 userId + profileVersion 读取用户画像 Chunks
   ↓
[分析] Prompt = 岗位JD + 前端规则结果 + 召回画像  → 大模型生成匹配分析
   ↓
[落库] 结果写入 MySQL + Redis → 返回前端 → 投递反馈反哺更新画像版本
```

### 1.3 面试官常见追问

- **Q：为什么 Userscript 直接在页面采集，而不是后端爬虫？**
  - A：① 规避反爬与法律风险，数据由用户主动触发采集；② 复用用户登录态，能拿到登录后可见的完整 JD；③ 减少后端维护 Cookie/代理池的成本。
- **Q：为什么用户简历不在每次请求里传递？**
  - A：减少传输负担 + 避免前端篡改画像。后端按 `userId` 动态查询，保证画像一致性。
- **Q：为什么拆用户、画像块、岗位、分析结果、反馈为多张表？**
  - A：**生命周期不同**。画像块随简历更新频繁变更，岗位记录随采集增长，分析结果依赖前两者快照，反馈是后续追加。拆表便于按类型/版本检索、增量更新、数据审计。

---

## 二、核心接口与数据结构（P0 致命项 · 必须从源码抄出）

### 2.1 后端接口契约

| 项 | 值 |
| :--- | :--- |
| Path | `POST /api/job/analyze` |
| Content-Type | `application/json` |
| 请求体字段 | 岗位标题、公司、城市、薪资、出勤要求、实习周期、完整 JD 文本、页面来源、前端规则总分、规则评分明细 |
| 响应体字段 | 匹配评分、匹配依据、技术缺口、风险项、投递建议、面试建议 |

> **关键设计点**：请求中**不携带用户简历**，后端按当前用户 ID 动态查询画像。

### 2.2 真实 DTO 字段表（[需核对] 从代码填充）

```
JobAnalyzeRequest:
  - [字段名] : [类型]   // [说明]
  - ...

JobAnalyzeResponse:
  - [字段名] : [类型]   // [说明]
  - ...
```

> ⚠️ 作答时严禁混淆 `Java 的 String` 与 `MySQL 的 VARCHAR`。
> Java 侧是语言类型，MySQL 侧是列类型，二者映射需逐字段说明（如 `String → VARCHAR(255)` / `String → TEXT`）。

---

## 三、Redis 缓存设计（P2 必答项 · 全参数表）

### 3.1 缓存参数清单（[需核对] 逐项确认）

| 参数 | 设计值 / 说明 | [需核对] |
| :--- | :--- | :--- |
| Key 前缀 | `job:analysis:` | |
| Key 完整结构 | `job:analysis:{profileVersion}:{jobHash}` | |
| jobHash 算法 | SHA-256(岗位标题 + 公司 + 城市 + 标准化 JD 文本) | |
| Hash 输入字段 | 岗位标题、公司、城市、标准化 JD 文本 | |
| Hash 目的 | 控制 Key 长度 + 唯一标识岗位 | |
| Value 格式 | 结构化岗位分析结果 JSON | |
| Value 序列化方式 | [需核对：String / JSON / Jackson] | ☐ |
| TTL | [需核对：具体秒数/分钟数] | ☐ |
| 命中流程 | 组合 Key 查 Redis → 命中直接返回，不查 DB | |
| 失效策略 | 画像更新 → profileVersion 自增 → 旧 Key不命中 → 等 TTL 自然过期 | |

### 3.2 核心设计决策（"为什么"必答）

- **Q：为什么用 `profileVersion` 进 Key 而不是画像内容 hash？**
  - A：画像内容 hash 计算成本高且粒度太细（任何字段变动都失效）；`profileVersion` 是显式版本号，画像更新时只需 +1，旧缓存自然失配，**无需批量删除**，运维简单。
- **Q：为什么 jobHash 用 SHA-256 而不是 MD5？**
  - A：SHA-256 碰撞概率更低，安全性与唯一性更可靠；MD5 已知存在碰撞风险。
- **Q：为什么不让前端传简历，反而后端查 + 缓存？**
  - A：① 减少请求体大小；② 画像在后端集中管理，版本可控；③ 缓存 Key 含 profileVersion 保证同岗位在不同画像版本下结果隔离，避免脏缓存。

---

## 四、数据库表结构（P0 致命项 · 必须抄真实列名）

### 4.1 profile_chunk 表核心字段（[需核对] 从 DDL 填真实列名与类型）

| 字段名 | Java 类型 | MySQL 类型 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | Long | BIGINT | 主键 |
| `user_id` | Long | BIGINT | 用户 ID |
| `profile_version` | Long | BIGINT | 画像版本号 |
| `chunk_type` | String | VARCHAR | 块类型（如 `PROJECT_EXPERIENCE`） |
| `chunk_text` | String | **TEXT / LONGTEXT** | 块文本（注意：不是 VARCHAR） |
| `keywords` | JSON | JSON | 关键词及权重 `[{"term":"Spring Boot","weight":1.0}]` |
| `metadata` | JSON | JSON | 元数据（来源、项目名等） |
| `embedding` | [需核对] | [需核对] | 向量字段（若存储在 DB） |

### 4.2 设计原则问答

- **Q：为什么 chunk_text 用 TEXT/LONGTEXT 而不是 VARCHAR？**
  - A：项目经历/实习描述可能超长，VARCHAR 受行最大字节限制（约 65535，且受字符集影响），TEXT 是独立存储页，不受行长度限制，适合长文本。
- **Q：为什么 keywords 和 metadata 用 JSON 类型？**
  - A：结构灵活、支持半结构化查询（MySQL 5.7+ 支持 JSON 函数），避免为关键词单独建子表带来的 JOIN 开销。
- **Q：embedding 存 MySQL 还是专门向量库？**
  - A：[需核对] 若项目用专门向量库（如 Milvus/pgvector），MySQL 仅存元数据；若轻量方案可存 MySQL JSON 列。**不可混淆 Embedding 模型（生成向量）与向量数据库（存储向量）**。

---

## 五、前端规则评分（P1 必答项 · 真实正则与权重）

### 5.1 评分维度（[需核对] 从 Userscript 源码抄真实权重）

| 维度 | 字段名 | 真实权重 [需核对] | 正则/匹配规则 [需核对] | 命中示例 | 扣分示例 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 岗位方向 | directionScore | ☐ | ☐ | | |
| 技术栈 | skillScore | ☐ | ☐ | | |
| 毕业年份 | ☐ | ☐ | ☐ | | |
| 城市 | ☐ | ☐ | ☐ | | |
| 出勤天数 | ☐ | ☐ | ☐ | | |
| 实习周期 | ☐ | ☐ | ☐ | | |
| 学历要求 | ☐ | ☐ | ☐ | | |
| 风险关键词 | ☐ | ☐ | ☐ | | |

### 5.2 输出结构（必须含明细，非仅总分）

```json
{
  "totalScore": 82,
  "details": { "directionScore": 20, "skillScore": 28 },
  "matchedRules": ["岗位方向匹配 AI 应用开发", "命中 Java、Redis 技能"],
  "riskRules": ["JD 中未明确毕业年份要求"]
}
```

### 5.3 硬性风险 vs 扣分（关键区分）

- **硬风险**（如"只招 27 届"、"实习周期不满足"）→ 标记为硬风险，**直接判定不匹配**，而非简单扣分。
- **软扣分**（如"城市非目标但可接受"）→ 仅扣分。

> ⚠️ **禁止编造权重**，所有数值必须来自 Userscript 源码常量。

---

## 六、混合检索（P4 深化项 · 全参数）

### 6.1 关键词检索

- **分数公式**：`keywordScore = Σ(命中关键词权重) / Σ(查询关键词总权重)`，归一化到 0~1。
- **示例**：查询词 `[Java(1.0), Redis(0.8), Python(0.7)]`，Chunk 命中 Java+Redis → `(1.0+0.8)/(1.0+0.8+0.7) ≈ 0.63`。

### 6.2 向量检索

- **Embedding 模型**：[需核对：名称]
- **向量维度**：[需核对]
- **向量存储位置**：[需核对：MySQL / pgvector / Milvus / Redis]
- **相似度算法**：余弦相似度
- **Top-K**：[需核对]

### 6.3 融合公式（[需核对] α/β/γ 真实数值）

```
finalScore = α * semanticScore + β * keywordScore + γ * metadataScore
（三者先归一化，α + β + γ = 1）
```

### 6.4 并行 vs 串行（必须按真实代码回答）

| 模式 | 说明 | 项目实际 [需核对] |
| :--- | :--- | :--- |
| 并行 | 两路同时召回 → 去重 → 归一化 → 融合 | ☐ |
| 串行（两阶段） | 关键词粗召回 → 向量重排 | ☐ |

> **设计理由**（若为并行）：关键词保证硬术语（Java）不漏，向量召回语义相近内容，两者互补，避免关键词硬过滤导致语义相关数据丢失。

### 6.5 认知陷阱（必避）

- ❌ 混淆 **Embedding 模型**（生成向量）与 **向量数据库**（存储向量）。
- ❌ 把"已实现的规则检索"说成"已上线的完整混合检索/BM25"。

---

## 七、Docker 容器化（区分实际运行 vs 配置）

| 维度 | 真实情况 [需核对] |
| :--- | :--- |
| 实际运行容器 | ☐ 仅 Redis / ☐ 全栈 |
| 仓库内 docker-compose.yml | ☐ 有 / ☐ 无，覆盖哪些服务 |
| 开发环境启动方式 | ☐ `docker run` / ☐ `docker-compose up` |

> ⚠️ 不可笼统说"所有服务都容器化"。必须区分：实际跑的容器 vs 仓库里准备好的编排配置。

---

## 八、用户画像构建链路（P3 核心项 · [需核对]）

| 环节 | 真实方案 [需核对] |
| :--- | :--- |
| 简历输入格式 | ☐ PDF / ☐ Word / ☐ 在线表单 |
| PDF 解析工具 | ☐（如 Apache PDFBox / pdfplumber / 自研） |
| 结构化抽取方式 | ☐ 规则正则 / ☐ 大模型抽取 / ☐ 混合 |
| Chunk 拆分规则 | ☐ 按简历章节 / ☐ 按固定长度 / ☐ 按语义 |
| 画像版本自增触发 | ☐ 简历更新 / ☐ 投递反馈反哺 / ☐ 手动 |

---

## 九、避坑清单（面试暴露的认知缺陷）

1. **概念混淆**
   - Java `String` ≠ MySQL `VARCHAR`（语言类型 vs 列类型）。
   - Embedding 模型（生成向量）≠ 向量数据库（存储向量）。
2. **未区分"已实现"与"规划中"**
   - 规则检索 = 已上线；完整混合检索/BM25 = 待优化。
3. **缺乏"为什么"的思考**
   - 不只说"用了 Redis/MySQL"，要答：为什么这张表？为什么这个 Key？解决了什么问题？

---

## 十、补强优先级清单（按紧急度执行）

| 优先级 | 任务 | 产出物 | 状态 |
| :--- | :--- | :--- | :--- |
| **P0 致命** | 抄真实 DTO 与表结构 | `JobAnalyzeRequest/Response` 字段、`profile_chunk` 真实列名类型 | ☐ |
| **P1 必答** | 整理真实评分规则表 | 维度、权重、正则、命中/扣分示例、硬风险条件 | ☐ |
| **P2 重要** | 整理 Redis 全参数 | Key 前缀、Hash 输入、Value 序列化、TTL、命中失效流程 | ☐ |
| **P3 核心** | 画像构建链路 | PDF 解析工具、抽取方式、Chunk 拆分规则 | ☐ |
| **P4 深化** | 检索链路全参数 | Embedding 模型、维度、存储位置、α/β/γ、Top-K | ☐ |

---

## 附：万能追问应答框架（STAR + 设计权衡）

每个技术点回答时套用：

1. **是什么**：直接说方案/字段/数值。
2. **为什么这么设计**：解决什么问题、对比过什么替代方案。
3. **怎么实现**：关键代码/配置/数据流。
4. **权衡与边界**：优点、缺点、已知限制、未来优化方向。
5. **真实数据**：TTL、维度、权重等具体数值（[需核对] 项必须先查代码）。
